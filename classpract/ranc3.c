//In this we'll be trying to get multiple outputs
//Using a single printf() function 

#include <stdio.h>

int main() {
    int first_integer = 98;
    char first_charp[] = "this is an age and it's very hard to achieve: ";
    printf("%s %d", first_charp, first_integer);
}